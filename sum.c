#include<stdio.h>
int  main()
{
    int num1, numm2, sum=0;
    printf("Enter number 1 to add:");
    scanf("%d", &num1);
    printf("Enter number 2 to add:");

    scanf("%d", &num2);
    sum =  sum + num1  + num2;
    printf("the sum is %d", sum);

    return 0;
}
